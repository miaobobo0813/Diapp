<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Diapp.Key"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Diapp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid x:Name="shadowCard"/>
        <Border CornerRadius="5" 
                Translation="0,0,30" 
                Loaded="Border_Loaded" 
                Width="750" Height="250" 
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <Border.Shadow>
                <ThemeShadow x:Name="shadow"/>
            </Border.Shadow>
            <StackPanel Spacing="10" Margin="20" 
                        HorizontalAlignment="Center" 
                        VerticalAlignment="Center">
                <TextBlock Text="密钥" FontWeight="Bold" FontSize="25"/>
                <TextBlock Text="这些信息将用于加密和解密日记，确保信息准确无误，否则日记将显示为乱码。请使用至少10位的密码。"/>
                <PasswordBox x:Name="UserName"
                             PlaceholderText="输入用户名..."/>
                <PasswordBox x:Name="Password"
                             PlaceholderText="输入密码..."/>
                <Button x:Name="SavePassword"
                        Content="保存密钥"
                        Click="SavePassword_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
