<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Diapp.DiaryList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Diapp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    
    <ScrollViewer>
        <StackPanel Padding="40" Spacing="20">
            <TextBlock Text="日记列表" FontSize="25" FontWeight="Bold"/>
            <GridView x:Name="diariesStack" 
                      IsItemClickEnabled="True">
                <GridView.ItemTemplate>
                    <DataTemplate x:DataType="local:DiaryForDisplay">
                        <Border Width="250" Height="100"
                                    CornerRadius="8"
                                    BorderThickness="1"
                                    Tapped="Border_Tapped"
                                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                            <StackPanel Spacing="8"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center">
                                <TextBlock Text="{x:Bind ShowTitle, Mode=OneTime}"
                                           FontWeight="SemiBold"
                                           TextWrapping="NoWrap"/>
                                <TextBlock Text="{x:Bind ShowDate, Mode=OneTime}"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </GridView.ItemTemplate>
            </GridView>
        </StackPanel>
        
    </ScrollViewer>
</Page>
